# Windows install is not ready yet
# But you'll need to create a conda or python environment
# Install pip install requirements.txt and optional-requirements.txt
# Also install pip install python-magic-bin semaphore_win_ctypes
# You may need to run as administator to download speech models for Whisper
